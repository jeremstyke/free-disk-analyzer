namespace FreeDiskAnalyzer.ViewModels;

/// <summary>
/// Marker view model for the Privacy page. Content is static (mirrors
/// PRIVACY.md at the repo root) so there is nothing to bind beyond
/// resolving the right view via the DataTemplate in App.xaml.
/// </summary>
public sealed class PrivacyViewModel
{
}
